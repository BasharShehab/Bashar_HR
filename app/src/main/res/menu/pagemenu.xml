<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/itemAddEmployee"
        android:icon="@drawable/ic_addemployee"
        android:title="@string/add_new_employee"
        app:showAsAction="ifRoom|collapseActionView"
        />
    <item android:id="@+id/itemRemoveEmployee"
        android:icon="@drawable/ic_employeeremove"
        android:title="@string/remove_employee"
        app:showAsAction="ifRoom|collapseActionView"
        />
    <item android:id="@+id/about"
        android:icon="@drawable/ic_addemployee"
        android:title="@string/about"
        app:showAsAction="ifRoom|collapseActionView"
        />
    <item android:id="@+id/search_button"
        android:icon="@drawable/ic_search"
        android:title="@string/search"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="android.widget.SearchView" />
</menu>